const prefix = '/api/history'

const historyBase = {
  //获取历史传感记录
  getHistoryInfo: prefix + '/info',
  // //创建历史传感记录
  // createSensor: prefix + '/create',
  // //删除历史传感记录
  // deleteSensor: prefix + '/delete'
}

export default historyBase